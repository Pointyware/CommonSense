package org.pointyware.commonsense.feature.ontology.ui

import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import org.pointyware.commonsense.feature.ontology.viewmodels.ClassEditorUiState

/**
 * Allows a user to create a new type of class.
 */
@Composable
fun ClassEditorScreen(
    state: ClassEditorUiState,
    modifier: Modifier = Modifier,
) {
    /*
    TODO: implement UI
      1. Create a text field for the class name
      2. Create a button to add a new field - allow user to add new field
      3. Create a list of fields
        1. create name input for field name
        2. create type dropdown for field type
     */
}
